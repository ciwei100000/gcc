# DP: updates from the 10 branch upto 20200507 (documentation).

LANG=C git diff --no-renames --src-prefix=a/src/ --dst-prefix=b/src/ \
        6e6e3f144a33ae504149dc992453b4f6dea12fdb 6032350c350b72859f1280e0eb13c8af4c522422\
	| awk '/^diff .*\.texi/ {skip=0; print; next} /^diff / {skip=1; next} skip==0' \
        | grep -v -E '^(diff|index)'

