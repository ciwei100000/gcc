# DP: updates from the 10 branch upto 20200525 (documentation).

LANG=C git diff --no-renames --src-prefix=a/src/ --dst-prefix=b/src/ \
        6e6e3f144a33ae504149dc992453b4f6dea12fdb ca502d7b3c16ef9fa007263aee078b1e451c1eb4 \
	| awk '/^diff .*\.texi/ {skip=0; print; next} /^diff / {skip=1; next} skip==0' \
        | grep -v -E '^(diff|index)'

