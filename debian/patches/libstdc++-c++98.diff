# DP: Don't use C++11 syntax in file built in C++98 mode.

--- a/src/libstdc++-v3/src/c++98/ios_failure.cc
+++ b/src/libstdc++-v3/src/c++98/ios_failure.cc
@@ -123,7 +123,7 @@
     return __class_type_info::__do_upcast(dst_type, obj_ptr);
   }
 #else // _GLIBCXX_USE_DUAL_ABI && __cpp_rtti
-  using __iosfailure = ios::failure;
+  typedef ios::failure __iosfailure;
 #endif
 
   void
